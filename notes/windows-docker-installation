1. git clone https://github.com/dockur/windows
```yaml
services:
  windows:
    image: dockurr/windows
    container_name: windows
    environment:
      VERSION: "10"
      RAM_SIZE: "2.3G"
      CPU_CORES: "4"
      KEYBOARD: "ru-RU"
    devices:
      - /dev/kvm
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
      - 3389:3389/tcp
      - 3389:3389/udp
    volumes:
      - ./windows:/storage
      - ./shared:/data
    restart: always
    stop_grace_period: 2m
```
2. docker-compose up -v
3. Connect over RDP:3389 login:docker,password:admin (remmina)
